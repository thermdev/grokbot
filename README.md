# discord bot tool with genai capabilities
hi thermite here meow meow meow

## tutorial

commands:

1. @grok is this true

## installation


how run:

1. conda create -n discord --file requirements.txt
2. conda activate discord
3. python3 test.py

so you'll need in .env:

- BOT_TOKEN=\<your discord token>
- XAI_API_KEY=\<idk if this is used>
- DEEPL_API_KEY=\<deepl translate api (use the non-free if u have it)>
- GEMINI_API_KEY=\<gemini api key>